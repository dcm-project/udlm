# core-services — a BUNDLING node. It declares the shared platform dependencies (idm + dns) ONCE;
# any resource that depends on core-services inherits them as SECONDARY dependencies, via the
# graph's transitivity. No special "bundle" type is needed — to bundle a set of dependencies you
# simply declare them on a node and depend on that node; the topological order carries the rest.
# (Contrast the hosts, which get the same identity/DNS a different way — scope-derived from tenant.)
uuid: 30fe47cb-e61b-490d-8657-a56c76581a4f
handle: core-services
conforms_to: udlm/0.1
resource_type: Software.Service
type_version: 0.1.0
type_ref: https://udlm.dev/registry/udlm/0.1/Software.Service/0.1.0
lifecycle_state: Discovered
tenant_uuid: 14230477-819f-4e9b-bfec-3fcec6ee3b79
states:
  discovered:
    fields:
      service_kind: infrastructure-daemon
    at: '2026-07-13T00:00:00Z'
dependencies:
- edge_type: depends_on
  target_uuid: e787a045-a475-48a8-890a-4a369d15b535
  target_handle: idm
- edge_type: depends_on
  target_uuid: 1b70eee7-3169-457a-a805-a91766823292
  target_handle: dns
