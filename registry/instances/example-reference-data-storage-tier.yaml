# A REFERENCE DATA LAYER (layering-and-versioning §3.7) — one canonical term of the `storage_tier`
# value vocabulary that the Compute Base Class's `storage_tier` SharedDataElement is governed by
# (registry/classes/compute.yaml; ADR-038 §2). Each tier is a governed reference_data record; the
# vocabulary is the set of them (standard / performance / archive), curated proposed→canonical.
# An instance's storage_tier is a bare string (permissive profiles) or a data_reference to one of
# these canonical terms (strict profiles) — the profile-driven rigor (ADR-007 / ADR-039).
record_type: layer
uuid: bae49d04-7924-4cf5-8f22-24d01091c609
handle: platform/storage-tiers/performance
conforms_to: udlm/0.1
name: Storage tier — performance
description: Governed definition of the 'performance' storage tier — the shared source of truth for its SLA floor.
version: 1.0.0
tenant_uuid: 75ccf4ff-3e8d-4963-bc51-459ae1014cb7
status:
  state: active
layer_type: reference_data
reference_data_type: storage_tier
domain: platform
contributor:
  contributor_type: platform_admin
  review: dual_approval
fields:
  tier_code: performance
  min_iops: 20000
  min_throughput_mbps: 500
  description: Low-latency SSD/NVMe-class backing for latency-sensitive workloads.
