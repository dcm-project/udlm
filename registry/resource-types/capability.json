{
  "$id": "https://udlm.dev/registry/udlm/0.1/Capability/0.1.0",
  "conforms_to": "udlm/0.1",
  "uuid": "6f097f82-9ed7-4be5-ae20-eca1b8e30051",
  "resource_type": "Capability",
  "version": "0.1.0",
  "family": "Knowledge",
  "entity_type": "Capability",
  "lifecycle_archetype": "curation",
  "portability": "portable",
  "status": "active",
  "metadata": {
    "display_name": "Capability",
    "description": "A discrete platform capability — the unit of architectural capability tracked, normalized, and gap-analysed by DAV. A Knowledge-family entity: its lifecycle is curation (proposed -> under-review -> canonical -> deprecated), not provisioning, and it is never realized by a provider. The family tag disambiguates the collision-prone bare word 'Capability' from any Resource type.",
    "maintainer": "dav",
    "origination_timestamp": "2026-06-18T00:58:56Z"
  },
  "spec": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "name",
      "description"
    ],
    "properties": {
      "name": {
        "type": "string",
        "description": "Canonical capability name (single normalized handle). The curation lifecycle converges aliases onto this."
      },
      "description": {
        "type": "string",
        "description": "What the capability is and the architectural concern it addresses."
      },
      "pillar": {
        "type": "string",
        "description": "Top-level grouping the capability rolls up to (e.g. a maturity-framework pillar)."
      },
      "domain": {
        "type": "string",
        "description": "Capability domain (e.g. one of the DCM capability domains) this capability belongs to."
      },
      "curation_state": {
        "enum": [
          "proposed",
          "under-review",
          "canonical",
          "deprecated"
        ],
        "description": "Position in the curation lifecycle — the Knowledge-family interpretation of the four states (provenance-first; promotion to canonical is a curation act, applied by Policy, not by a provider)."
      }
    }
  },
  "immutable": [],
  "outputs": {
    "normalized_handle": {
      "type": "string",
      "description": "The canonical handle a Capability resolves to once curated to `canonical`. Other Knowledge entities (aliases, use cases) bind to this via typed references rather than free-text capability names."
    }
  },
  "relationships": [
    {
      "kind": "references",
      "target": "TaxonomyTerm",
      "target_field": "termHandle",
      "cardinality": "0..n",
      "description": "normalized_to — this capability is mapped to one or more canonical taxonomy terms (e.g. the DCM capability taxonomy). A declarative typed reference; the mapping act is Policy."
    },
    {
      "kind": "depends_on",
      "target": "Capability",
      "cardinality": "0..n",
      "description": "This capability depends on another capability (capability-map edge). Forms an acyclic capability DAG; traversal/ordering is Policy."
    }
  ]
}
