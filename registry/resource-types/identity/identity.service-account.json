{
  "$id": "https://udlm.dev/registry/udlm/0.1/Identity.ServiceAccount/0.3.3",
  "conforms_to": "udlm/0.1",
  "uuid": "f3e83ea0-a5d5-49fa-937b-a4d3fecdf74c",
  "resource_type": "Identity.ServiceAccount",
  "version": "0.3.3",
  "family": "Access",
  "entity_type": "Identity",
  "status": "active",
  "metadata": {
    "display_name": "Service Account",
    "description": "A non-human identity/account — an ACTOR (automation, an agent, a provider integration, an API-key holder) that DCM authenticates and authorizes (ADR-RBAC-001). actor_type `service_account`. Holds no secret material — credentials referenced (Security.CredentialRef). Backs the built-in provider's static_api_key/service accounts.",
    "maintainer": "udlm-registry",
    "origination_timestamp": "2026-07-11T00:00:00Z"
  },
  "context": {
    "purpose": "Models a non-human account — automation, an agent, a provider integration — as an authenticated, auditable actor.",
    "plain_description": "An account for something that is not a person: a pipeline, an agent, an integration holding an API key. Like a person it has a required `handle` and `actor_type` (always `service_account`), an authenticator, and referenced-only credentials; unlike a person it declares an `owner_ref` — the person or group accountable for it. Its stable actor id is what role assignments and audit records bind to.",
    "use_when": [
      "You need automation to authenticate with its own identity instead of borrowing a person's.",
      "You need every non-human credential holder to have a named accountable owner."
    ],
    "not_for": [
      "A human account — Identity.Person.",
      "The API key or token itself — Security.CredentialRef, referenced not stored."
    ],
    "works_with": [
      "Identity.Person — the accountable owner.",
      "Security.CredentialRef — the account's key/token, by reference.",
      "Identity.Group — memberships that grant it roles."
    ]
  },
  "spec": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "handle",
      "actor_type"
    ],
    "properties": {
      "handle": {
        "type": "string",
        "description": "Service-account name, unique within the implementation."
      },
      "display_name": {
        "type": "string"
      },
      "actor_type": {
        "const": "service_account",
        "description": "From the closed actor-type vocabulary (universal-audit)."
      },
      "owner_ref": {
        "description": "Reference to Identity.Person or Identity.Group — the owning identity accountable for this account. Common-elements Reference shape (handle string or {uuid, handle} object); resolvable and policy-walkable, never an opaque string.",
        "oneOf": [
          {
            "type": "string",
            "description": "Short form — the handle of an existing Platform.StorageClass."
          },
          {
            "$ref": "../../common-elements.schema.json#/$defs/Reference"
          }
        ]
      },
      "authenticated_by": {
        "type": "string",
        "description": "Auth Provider that authenticates it (built-in by default)."
      },
      "credential_ref": {
        "description": "Reference to Security.CredentialRef — the credential record this account authenticates with. Common-elements Reference shape (handle string or {uuid, handle} object); resolvable and policy-walkable, never an opaque string.",
        "oneOf": [
          {
            "type": "string",
            "description": "Short form — the handle of an existing Platform.StorageClass."
          },
          {
            "$ref": "../../common-elements.schema.json#/$defs/Reference"
          }
        ]
      },
      "status": {
        "enum": [
          "active",
          "suspended",
          "disabled"
        ],
        "default": "active"
      }
    },
    "examples": [
      {
        "handle": "backup-agent",
        "display_name": "Backup Agent",
        "actor_type": "service_account",
        "owner_ref": "platform-admins",
        "authenticated_by": "corp-directory",
        "credential_ref": "backup-agent-token",
        "status": "active"
      }
    ]
  },
  "outputs": {
    "actor_uuid": {
      "type": "string",
      "description": "The stable actor id referenced by role-assignments and DCMGroup (access_grouping) membership."
    }
  },
  "relationships": [
    {
      "edge_type": "contained_by",
      "target": "Identity.Group",
      "cardinality": "0..n",
      "description": "Member of an access DCMGroup / Identity.Group; membership drives role binding."
    },
    {
      "edge_type": "references",
      "target": "Security.CredentialRef",
      "cardinality": "0..n",
      "description": "Credentials — referenced, never inline."
    }
  ],
  "coverage": {
    "use_cases": [
      "observed/estate-resource-observed",
      "observed/observation-off-vocabulary-quarantined",
      "audit/refusal-emits-audit-record"
    ],
    "flows": [
      "docs/flows/estate-observation-lifecycle.md",
      "docs/flows/uc-07-udlm-dependency-graph-data-model.md"
    ]
  }
}
