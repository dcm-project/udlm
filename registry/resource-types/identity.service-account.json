{
  "$id": "https://udlm.dev/registry/udlm/0.1/Identity.ServiceAccount/0.1.0",
  "conforms_to": "udlm/0.1",
  "uuid": "f2d8b5e0-4c73-4a19-8b26-3e7f9d1c8042",
  "resource_type": "Identity.ServiceAccount",
  "version": "0.1.0",
  "family": "Access",
  "entity_type": "Identity",
  "lifecycle_archetype": "provisioning",
  "portability": "portable",
  "status": "active",
  "metadata": {
    "display_name": "Service Account",
    "description": "A non-human identity/account — an ACTOR (automation, an agent, a provider integration, an API-key holder) that DCM authenticates and authorizes (ADR-RBAC-001). actor_type `service_account`. Holds no secret material — credentials referenced (Security.CredentialRef). Backs the built-in provider's static_api_key/service accounts.",
    "maintainer": "udlm-registry",
    "origination_timestamp": "2026-07-11T00:00:00Z"
  },
  "spec": {
    "type": "object",
    "additionalProperties": false,
    "required": ["handle", "actor_type"],
    "properties": {
      "handle": { "type": "string", "description": "Service-account name, unique within the realization." },
      "display_name": { "type": "string" },
      "actor_type": { "const": "service_account", "description": "From the closed actor-type vocabulary (universal-audit)." },
      "owner_ref": { "type": "string", "description": "Owning Identity.Person or team (accountability)." },
      "authenticated_by": { "type": "string", "description": "Auth Provider that authenticates it (built-in by default)." },
      "credential_ref": { "type": "string", "description": "Reference to a Security.CredentialRef (API key / token) — never inline." },
      "status": { "enum": ["active", "suspended", "disabled"], "default": "active" }
    }
  },
  "outputs": {
    "actor_uuid": { "type": "string", "description": "The stable actor id referenced by role-assignments and DCMGroup (access_grouping) membership." }
  },
  "relationships": [
    { "kind": "contained_by", "target": "Group", "cardinality": "0..n", "description": "Member of an access DCMGroup / Identity.Group; membership drives role binding." },
    { "kind": "references", "target": "Security.CredentialRef", "cardinality": "0..n", "description": "Credentials — referenced, never inline." }
  ]
}
