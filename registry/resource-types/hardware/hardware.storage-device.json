{
  "$id": "https://udlm.dev/registry/udlm/0.1/Hardware.StorageDevice/0.2.2",
  "conforms_to": "udlm/0.1",
  "uuid": "b9b08521-7927-479c-8671-6d4fdadc9f1b",
  "resource_type": "Hardware.StorageDevice",
  "version": "0.2.2",
  "family": "Resource",
  "portability": "portable",
  "status": "active",
  "metadata": {
    "display_name": "Storage Device",
    "description": "A storage device (disk / SSD / NVMe) component. device_class distinguishes a physical drive (wwn/serial/bay) from a virtual disk presented to a guest (which binds_to its backing Storage resource). Mirrors Redfish Drive (+ SNIA Swordfish at the system level).",
    "maintainer": "udlm-registry",
    "origination_timestamp": "2026-06-27T00:11:51Z"
  },
  "context": {
    "purpose": "Inventories a disk/SSD/NVMe — physical drive or virtual disk — with the identity (WWN, serial, bay) that ties failures and replacements to one device.",
    "plain_description": "One storage device as a record: its required `capacity` — a whole-number quantity like 4TB; fractional sizes do not validate — its `media_type` and bus `protocol`, and where and what it is. Identity facts — bay or slot (`location`), model, serial, WWN, and the semantic `role` (boot, data, cache, a storage daemon's member drive) — nest under the `identity` block, not at the top level. device_class separates a physical drive from a virtual disk presented to a guest; a virtual disk points at the storage that backs it. Once realized, the OS device path is published, tying the inventory record to what the host sees.",
    "use_when": [
      "You need drive-level inventory — which drive, in which bay, of which host — so a failing disk maps to a physical pull-and-replace.",
      "You need pool or cluster membership grounded in real devices (a vdev's members, a storage daemon's backing drive)."
    ],
    "not_for": [
      "The consumable volume a workload attaches — Storage.Volume; a device is hardware, a volume is provisioned capacity.",
      "The aggregation layer over drives — Storage.Pool (host-local) or Storage.Cluster (distributed)."
    ],
    "works_with": [
      "Compute.BareMetalHost — the host the drive is installed in.",
      "Storage.Pool — pools whose vdevs group these drives.",
      "Compute.VirtualMachine — the guest a virtual disk is presented to."
    ]
  },
  "spec": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "capacity"
    ],
    "properties": {
      "device_class": {
        "type": "string",
        "enum": [
          "physical",
          "virtual",
          "passthrough",
          "partition"
        ],
        "default": "physical",
        "description": "Device realization (common-elements.md §7). virtual = a guest virtual disk (binds_to its backing volume); passthrough = whole physical disk to a guest."
      },
      "partition_mechanism": {
        "type": "string",
        "description": "Only when device_class=partition: e.g. namespace (NVMe) / lun carved from a parent."
      },
      "identity": {
        "type": "object",
        "description": "Instance discriminators (SPEC-DESIGN-REQUIREMENTS §27).",
        "properties": {
          "location": {
            "type": "string",
            "description": "Drive bay / slot within the parent, e.g. Bay 7 (Redfish PhysicalLocation). Primary discriminator."
          },
          "serial_number": {
            "type": "string",
            "description": "Drive serial (Redfish SerialNumber)."
          },
          "wwn": {
            "type": "string",
            "description": "World-Wide Name — globally unique block-device identity."
          },
          "asset_tag": {
            "type": "string"
          },
          "model": {
            "type": "string",
            "description": "Drive model (Redfish Model)."
          },
          "manufacturer": {
            "type": "string"
          },
          "role": {
            "type": "string",
            "description": "Semantic usage, e.g. boot | data | ceph-osd | cache."
          }
        }
      },
      "capacity": {
        "type": "string",
        "pattern": "^[0-9]+(MB|GB|TB|PB|MiB|GiB|TiB|PiB)$",
        "description": "Usable capacity, e.g. 1TB (Redfish CapacityBytes)."
      },
      "media_type": {
        "type": "string",
        "description": "Media, e.g. hdd, ssd, nvme (Redfish MediaType)."
      },
      "protocol": {
        "type": "string",
        "description": "Bus/transport, e.g. sas, sata, nvme (Redfish Protocol)."
      },
      "rotation_speed_rpm": {
        "type": "integer",
        "minimum": 0,
        "description": "Spindle speed for rotational media; 0 / omitted for SSD/NVMe."
      }
    },
    "examples": [
      {
        "device_class": "physical",
        "identity": {
          "location": "Bay 3",
          "serial_number": "DRV-SN-EXAMPLE-0007",
          "wwn": "naa.5000000000000001",
          "model": "Example NVMe SSD 1.92TB",
          "manufacturer": "Example Storage Co.",
          "role": "ceph-osd"
        },
        "capacity": "1920GB",
        "media_type": "nvme",
        "protocol": "nvme",
        "rotation_speed_rpm": 0
      }
    ]
  },
  "outputs": {
    "device_path": {
      "type": "string",
      "description": "OS device path once Realized, e.g. /dev/disk/by-id/...."
    }
  },
  "relationships": [
    {
      "target": "Compute.BareMetalHost",
      "cardinality": "0..1",
      "name": "host",
      "description": "The host this drive is installed in; parent carries the reconciled storage rollup (§26).",
      "edge_type": "contained_by"
    },
    {
      "target": "Compute.VirtualMachine",
      "cardinality": "0..1",
      "name": "guest",
      "description": "For device_class=virtual/passthrough, the guest the disk is presented to.",
      "edge_type": "contained_by"
    },
    {
      "target": "Storage.Cluster",
      "target_field": "block_storage_class",
      "cardinality": "0..1",
      "description": "For a virtual disk, the backing storage resource (block storage class) it is provisioned from.",
      "edge_type": "binds_to"
    },
    {
      "target": "Hardware.StorageDevice",
      "cardinality": "0..1",
      "name": "parent_device",
      "description": "parent_device — the physical disk a passthrough/partition (e.g. NVMe namespace) is derived from (common-elements §7).",
      "edge_type": "references"
    }
  ],
  "adopts": [
    {
      "standard": "Redfish",
      "version": "2024.4",
      "standard_name": "Drive",
      "role": "drive component vocabulary (capacity, media, protocol, wwn, serial, bay)",
      "source": "https://www.dmtf.org/standards/redfish",
      "license": "DMTF",
      "license_compatibility": "compatible-reference"
    },
    {
      "standard": "SNIA-Swordfish",
      "version": "1.2",
      "role": "storage-device modeling alignment at the system level",
      "source": "https://www.snia.org/forums/smi/swordfish",
      "license": "SNIA",
      "license_compatibility": "compatible-reference"
    }
  ],
  "coverage": {
    "use_cases": [
      "observed/estate-resource-observed",
      "observed/observation-off-vocabulary-quarantined",
      "audit/refusal-emits-audit-record"
    ],
    "flows": [
      "docs/flows/estate-observation-lifecycle.md",
      "docs/flows/uc-07-udlm-dependency-graph-data-model.md"
    ]
  }
}
