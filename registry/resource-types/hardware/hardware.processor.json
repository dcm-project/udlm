{
  "$id": "https://udlm.dev/registry/udlm/0.1/Hardware.Processor/0.2.2",
  "conforms_to": "udlm/0.1",
  "uuid": "418c9acb-3cd8-4e73-817d-8be2bdf13442",
  "resource_type": "Hardware.Processor",
  "version": "0.2.2",
  "family": "Resource",
  "portability": "portable",
  "status": "active",
  "metadata": {
    "display_name": "Processor",
    "description": "A CPU component. First-class entity form of the §26 component model — a parent carries the cpu rollup and MAY also contain Hardware.Processor entities. device_class distinguishes a physical socket from a virtual CPU presented to a guest. outputs-exempt: opaque inventory component — its attributes are Discovered-state facts on the containing host; it publishes no realize-time binding surface (rule 36(b) documented exclusion).",
    "maintainer": "udlm-registry",
    "origination_timestamp": "2026-06-27T00:11:51Z"
  },
  "context": {
    "purpose": "Inventories a CPU — a physical socket or a vCPU presented to a guest — as a first-class component when the host rollup is not enough.",
    "plain_description": "One processor as its own record: `cores` (required), `threads`, `architecture`, and its clock as `max_speed_mhz`. Identity facts — model, location (the socket designation), serial — nest under the `identity` block, not at the top level. Most estates only need the aggregate CPU numbers already carried on the host; this type exists for when a socket must be individually addressable (asset tracking, heterogeneous sockets). device_class distinguishes the physical socket from a virtual CPU presented to a guest. Nothing binds to a processor at runtime, so it publishes no outputs — it is inventory.",
    "use_when": [
      "You need per-socket inventory (exact model, position, serial) beyond the host's aggregate core/thread rollup.",
      "You need virtual CPUs presented to a guest tracked as components."
    ],
    "not_for": [
      "Ordinary capacity accounting — the cpu rollup on Compute.BareMetalHost covers that without per-socket records.",
      "GPUs and accelerators — Hardware.GraphicsProcessor."
    ],
    "works_with": [
      "Compute.BareMetalHost — the host the socket is installed in, which carries the reconciled rollup.",
      "Compute.VirtualMachine — the guest a virtual CPU is presented to."
    ]
  },
  "spec": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "cores"
    ],
    "properties": {
      "device_class": {
        "type": "string",
        "enum": [
          "physical",
          "virtual",
          "passthrough",
          "partition"
        ],
        "default": "physical",
        "description": "Device realization (common-elements.md §7). physical = a socket/package; virtual = vCPU."
      },
      "partition_mechanism": {
        "type": "string",
        "description": "Only when device_class=partition: e.g. cpu-pinning / cpu-set carved from a parent."
      },
      "identity": {
        "type": "object",
        "description": "Instance discriminators (SPEC-DESIGN-REQUIREMENTS §27).",
        "properties": {
          "location": {
            "type": "string",
            "description": "Socket designation within the parent, e.g. CPU1 (Redfish Socket). Primary discriminator."
          },
          "serial_number": {
            "type": "string"
          },
          "asset_tag": {
            "type": "string"
          },
          "model": {
            "type": "string",
            "description": "Processor model, e.g. AMD EPYC 7443P (Redfish Model)."
          },
          "manufacturer": {
            "type": "string"
          },
          "role": {
            "type": "string"
          }
        }
      },
      "cores": {
        "type": "integer",
        "minimum": 1,
        "description": "Physical core count (Redfish TotalCores)."
      },
      "threads": {
        "type": "integer",
        "minimum": 1,
        "description": "Hardware thread count (Redfish TotalThreads)."
      },
      "architecture": {
        "type": "string",
        "description": "ISA, e.g. x86-64, aarch64 (Redfish ProcessorArchitecture)."
      },
      "max_speed_mhz": {
        "type": "integer",
        "minimum": 1,
        "description": "Max clock in MHz (Redfish MaxSpeedMHz)."
      }
    },
    "examples": [
      {
        "device_class": "physical",
        "identity": {
          "location": "CPU1",
          "model": "AMD EPYC 7443P",
          "manufacturer": "AMD",
          "role": "general-compute"
        },
        "cores": 24,
        "threads": 48,
        "architecture": "x86-64",
        "max_speed_mhz": 4035
      }
    ]
  },
  "outputs": {},
  "relationships": [
    {
      "target": "Compute.BareMetalHost",
      "cardinality": "0..1",
      "name": "host",
      "description": "The host this processor is installed in; parent carries the reconciled cpu rollup (§26).",
      "edge_type": "contained_by"
    },
    {
      "target": "Compute.VirtualMachine",
      "cardinality": "0..1",
      "name": "guest",
      "description": "For device_class=virtual, the guest the vCPU is presented to.",
      "edge_type": "contained_by"
    }
  ],
  "adopts": [
    {
      "standard": "Redfish",
      "version": "2024.4",
      "standard_name": "Processor",
      "role": "processor component vocabulary (cores, threads, architecture, speed, socket)",
      "source": "https://www.dmtf.org/standards/redfish",
      "license": "DMTF",
      "license_compatibility": "compatible-reference"
    }
  ],
  "coverage": {
    "use_cases": [
      "observed/estate-resource-observed",
      "observed/observation-off-vocabulary-quarantined",
      "audit/refusal-emits-audit-record"
    ],
    "flows": [
      "docs/flows/estate-observation-lifecycle.md",
      "docs/flows/uc-07-udlm-dependency-graph-data-model.md"
    ]
  }
}
