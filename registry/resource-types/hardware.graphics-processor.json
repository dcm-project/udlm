{
  "$id": "https://udlm.dev/registry/udlm/0.1/Hardware.GraphicsProcessor/0.1.0",
  "conforms_to": "udlm/0.1",
  "uuid": "8a9285ba-e17d-486b-aa81-37c7f94b16f9",
  "resource_type": "Hardware.GraphicsProcessor",
  "version": "0.1.0",
  "family": "Resource",
  "entity_type": "Infrastructure Resource",
  "portability": "portable",
  "status": "active",
  "metadata": {
    "display_name": "Graphics Processor",
    "description": "A GPU / accelerator component. device_class covers a physical GPU, a whole-GPU passthrough (VFIO) to a guest, or a vGPU/MIG partition carved from a physical GPU (parent_device references the physical Hardware.GraphicsProcessor). Mirrors Redfish Processor (ProcessorType=GPU) / PCIeDevice.",
    "maintainer": "udlm-registry",
    "origination_timestamp": "2026-06-26"
  },
  "spec": {
    "type": "object",
    "required": [],
    "properties": {
      "device_class": {
        "type": "string",
        "enum": ["physical", "virtual", "passthrough", "partition"],
        "default": "physical",
        "description": "Device realization (common-elements.md §7). passthrough = whole GPU to a guest; partition = vGPU/MIG slice (set parent_device + partition_mechanism)."
      },
      "partition_mechanism": {
        "type": "string",
        "description": "Only when device_class=partition: e.g. mediated (NVIDIA vGPU / Linux mdev), mig, sr-iov."
      },
      "identity": {
        "type": "object",
        "description": "Instance discriminators (SPEC-DESIGN-REQUIREMENTS §27).",
        "properties": {
          "location": { "type": "string", "description": "PCIe slot / BDF within the parent (Redfish PhysicalLocation). Primary discriminator; for a partition, the vGPU/MIG instance id." },
          "serial_number": { "type": "string", "description": "GPU serial (absent for virtual/partition)." },
          "asset_tag": { "type": "string" },
          "model": { "type": "string", "description": "GPU model, e.g. AMD Radeon Pro W7900 (Redfish Model)." },
          "manufacturer": { "type": "string" },
          "role": { "type": "string", "description": "Semantic usage, e.g. compute | display | inference." }
        }
      },
      "memory": {
        "type": "object",
        "properties": {
          "size": { "type": "string", "pattern": "^[0-9]+(MB|GB|TB|MiB|GiB|TiB)$", "description": "Framebuffer / device memory, e.g. 48GB (for a vGPU, the profile's allocation)." }
        }
      },
      "architecture": { "type": "string", "description": "GPU architecture / generation, e.g. RDNA4, Hopper." }
    }
  },
  "outputs": {},
  "relationships": [
    { "kind": "contained_by", "target": "Compute.BareMetalHost", "cardinality": "0..1", "description": "The host the physical GPU is installed in." },
    { "kind": "contained_by", "target": "Compute.VirtualMachine", "cardinality": "0..1", "description": "For passthrough / partition (vGPU), the guest the device is presented to." },
    { "kind": "references", "target": "Hardware.GraphicsProcessor", "cardinality": "0..1", "description": "parent_device — the physical GPU this passthrough/partition (vGPU/MIG) is derived from. REQUIRED when device_class is passthrough or partition (common-elements §7)." }
  ],
  "adopts": [
    {
      "standard": "Redfish",
      "version": "2024.4",
      "standard_name": "Processor",
      "role": "GPU/accelerator component vocabulary (Processor ProcessorType=GPU / PCIeDevice)",
      "source": "https://www.dmtf.org/standards/redfish",
      "license": "DMTF",
      "license_compatibility": "compatible-reference"
    }
  ]
}
