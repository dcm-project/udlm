{
  "$id": "https://udlm.dev/registry/udlm/0.1/Security.DirectoryService/0.4.3",
  "conforms_to": "udlm/0.1",
  "uuid": "b5a74015-f60e-49ff-9ff8-c704152cb958",
  "resource_type": "Security.DirectoryService",
  "version": "0.4.3",
  "family": "Resource",
  "status": "active",
  "metadata": {
    "display_name": "Directory Service",
    "description": "An identity directory server providing LDAP and (optionally) Kerberos. Grounded in RFC 4511/4512 (LDAP protocol + information model) and RFC 4120 (Kerberos). This is the directory SERVER (a Security resource); the identity DATA it holds (Person/Group/ServiceAccount) is the Identity information category. An integrated identity suite realizes this PLUS Network.DNSZone and (future) Security.CertificateAuthority.",
    "maintainer": "udlm-registry",
    "origination_timestamp": "2026-06-27T00:38:59Z"
  },
  "context": {
    "purpose": "Models the directory server — LDAP and optionally Kerberos — that identities authenticate against and services bind to.",
    "plain_description": "The identity directory as a running server: which `protocols` it serves — required; `ldap`, `ldaps`, `kerberos` — its `realm` and `base_dn`, and its role in a replication topology, spelled `replication_role` (`primary`, `replica`, `standalone`; a replica depends on its primary). Consumers get endpoints once realized — the LDAP URL, the Kerberos KDC, the base DN to bind under. The server is distinct from the identity data in it: people, groups, and service accounts are their own records; an integrated identity suite realizes this server plus DNS zones.",
    "use_when": [
      "You need services and hosts that authenticate against the directory to depend on it, so it stops last among them.",
      "You need the directory replication topology (primary/replica) explicit for recovery planning."
    ],
    "not_for": [
      "The identities inside — Identity.Person / Identity.Group / Identity.ServiceAccount.",
      "The DNS zones a directory suite serves — Network.DNSZone; related, but its own record.",
      "The bind credential — Security.CredentialRef."
    ],
    "works_with": [
      "Compute.VirtualMachine / Compute.BareMetalHost — where the directory runs.",
      "Identity.Group — external groups sourced from this directory.",
      "Software.Service — services requiring the directory, with hard/soft strength.",
      "Network.DNSZone — zones served when DNS is directory-integrated."
    ]
  },
  "spec": {
    "type": "object",
    "additionalProperties": false,
    "required": [
      "protocols"
    ],
    "properties": {
      "protocols": {
        "type": "array",
        "items": {
          "type": "string",
          "enum": [
            "ldap",
            "ldaps",
            "kerberos"
          ]
        },
        "description": "Directory access protocols served (RFC 4511 LDAP; RFC 4120 Kerberos)."
      },
      "realm": {
        "type": "string",
        "description": "Kerberos realm / identity realm, e.g. EXAMPLE.COM."
      },
      "base_dn": {
        "type": "string",
        "description": "LDAP base DN, e.g. dc=example,dc=com (RFC 4512 naming)."
      },
      "replication_role": {
        "type": "string",
        "enum": [
          "primary",
          "replica",
          "standalone"
        ],
        "default": "standalone",
        "description": "Topology role within the directory cluster."
      }
    },
    "examples": [
      {
        "protocols": [
          "ldap",
          "ldaps",
          "kerberos"
        ],
        "realm": "CORP.INTERNAL",
        "base_dn": "dc=corp,dc=internal",
        "replication_role": "primary"
      }
    ]
  },
  "outputs": {
    "ldap_endpoint": {
      "type": "string",
      "description": "LDAP(S) URL, e.g. ldaps://idm.example.com:636."
    },
    "kdc_endpoint": {
      "type": "string",
      "description": "Kerberos KDC endpoint, when Kerberos is served."
    },
    "base_dn": {
      "type": "string",
      "description": "Realized base DN consumers bind under."
    }
  },
  "relationships": [
    {
      "edge_type": "contained_by",
      "target": "Compute.VirtualMachine",
      "cardinality": "0..1",
      "name": "vm_host",
      "description": "The VM the directory server runs on."
    },
    {
      "edge_type": "contained_by",
      "target": "Compute.BareMetalHost",
      "cardinality": "0..1",
      "name": "metal_host",
      "description": "The bare-metal host the directory server runs on."
    },
    {
      "edge_type": "depends_on",
      "target": "Security.DirectoryService",
      "cardinality": "0..n",
      "name": "replication_peer",
      "description": "Replication peers (a replica depends_on its primary/peers)."
    }
  ],
  "adopts": [
    {
      "standard": "RFC-4511",
      "version": "4511",
      "standard_name": "Directory",
      "role": "LDAP protocol + directory information model vocabulary (RFC 4511/4512)",
      "source": "https://www.rfc-editor.org/rfc/rfc4511",
      "license": "IETF-Trust",
      "license_compatibility": "compatible-reference",
      "disposition": "tier1_value"
    },
    {
      "standard": "RFC-4120",
      "version": "4120",
      "standard_name": "KDC",
      "role": "Kerberos realm / KDC vocabulary",
      "source": "https://www.rfc-editor.org/rfc/rfc4120",
      "license": "IETF-Trust",
      "license_compatibility": "compatible-reference",
      "disposition": "tier1_value"
    }
  ],
  "aliases": [
    {
      "name": "Identity Management",
      "note": "FreeIPA/IdM (the suite realizes this + DNSZone + CA)"
    },
    {
      "name": "Active Directory Domain Services",
      "standard": "Microsoft",
      "note": "AD DS directory role"
    }
  ],
  "coverage": {
    "use_cases": [
      "security/integrate-directory-service",
      "security/reference-credential-just-in-time"
    ],
    "flows": [
      "docs/flows/credential-and-identity-lifecycle.md"
    ]
  }
}
